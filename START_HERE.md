# 🚀 Начните отсюда!

Добро пожаловать в **AIZoomDoc** — приложение для анализа строительных чертежей с помощью AI.

## За 5 минут до первого запроса

### Шаг 0: Проверьте Python версию (30 секунд)

```bash
python --version
```

**Рекомендуется:** Python 3.12.x  
**Текущее:** Python 3.13.5 (может работать, но менее стабильно)

Если нужна миграция → см. `PYTHON_VERSION.md`

### Шаг 1: Установка (2 минуты)

**Windows:**
```powershell
scripts\setup.bat
```

**Linux/Mac:**
```bash
chmod +x scripts/setup.sh
./scripts/setup.sh
```

Или используйте скрипт миграции:

**Windows (для миграции с 3.13 на 3.12):**
```powershell
migrate_to_python312.bat
```

**Linux/Mac:**
```bash
chmod +x migrate_to_python312.sh
./migrate_to_python312.sh
```

Или вручную:
```bash
python -m venv venv
venv\Scripts\activate      # Windows
source venv/bin/activate   # Linux/Mac
pip install -r requirements.txt
```

### Шаг 2: API ключ (1 минута)

1. Зарегистрируйтесь на [OpenRouter](https://openrouter.ai/)
2. Пополните баланс ($5 хватит на много запросов)
3. Скопируйте API ключ
4. Откройте `.env` и вставьте ключ:

```env
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь
```

### Шаг 3: Подготовьте данные (2 минуты)

Поместите в папку `data/`:

```
data/
├── result.md              # Ваш Markdown с OCR-текстом
├── annotation.json        # Ваш JSON с координатами
└── page_001_full.jpg      # Ваши изображения страниц
```

Примеры форматов см. в `data/README.md`

### Шаг 4: Первый запрос! (30 секунд)

```bash
python -m src.main "Найди всё вентиляционное оборудование" --data-root ./data
```

Если увидели ответ от LLM — всё работает! 🎉

## Что дальше?

### Базовые команды

```bash
# Простой поиск
python -m src.main "Найди АОВ8" --data-root ./data

# Сравнение стадий
python -m src.main "Сравни П и Р" --stage-p ./data/p --stage-r ./data/r

# Сохранение результата
python -m src.main "Анализ" --data-root ./data --output result.txt

# Другая модель
python -m src.main "Анализ" --data-root ./data --model "anthropic/claude-3.5-sonnet"
```

### Куда смотреть дальше?

| Хочу понять... | Читайте |
|----------------|---------|
| Как быстро начать | `QUICKSTART.md` |
| Основы использования | `README.md` |
| Примеры запросов | `USAGE_EXAMPLES.md` |
| Как это работает | `ARCHITECTURE.md` |
| Краткий обзор | `OVERVIEW.md` |

## Частые проблемы

### "OPENROUTER_API_KEY не задан"

✅ Проверьте `.env` файл:
```bash
type .env        # Windows
cat .env         # Linux/Mac
```

### "Файл не найден"

✅ Проверьте структуру данных:
```bash
dir data         # Windows
ls -la data      # Linux/Mac
```

Должны быть:
- `result.md`
- `annotation.json`
- `page_XXX_full.jpg`

### "Поиск не дал результатов"

✅ Проверьте, что в `result.md` есть ключевые слова:
- "вентиляция", "вентилятор", "АОВ" и т.д.

### "Таймаут при запросе"

✅ Попробуйте более быструю модель:
```bash
--model "google/gemini-2.0-flash-thinking-exp"
```

## Структура проекта (упрощённо)

```
aizoomdoc/
├── src/              # Код приложения (не трогать)
├── data/             # Ваши данные (сюда складывать файлы)
├── scripts/          # Скрипты установки
├── .env              # Настройки (API ключ)
└── *.md              # Документация (читать при необходимости)
```

## Логирование

Логи сохраняются в `aizoomdoc.log`. Если что-то не работает:

```bash
# Включите детальное логирование
# В .env установите:
LOG_LEVEL=DEBUG

# Просмотр логов в реальном времени
tail -f aizoomdoc.log      # Linux/Mac
Get-Content aizoomdoc.log -Wait -Tail 50  # Windows PowerShell
```

## Поддержка

- 📖 **Документация**: `README.md`, `QUICKSTART.md`
- 🐛 **Issues**: Создайте issue в репозитории
- 💡 **Примеры**: `USAGE_EXAMPLES.md`

## Checklist

Перед первым запуском убедитесь:

- [ ] Python 3.12+ установлен (`python --version`)
- [ ] Зависимости установлены (`pip list`)
- [ ] `.env` файл создан и содержит API ключ
- [ ] Папка `data/` содержит нужные файлы
- [ ] Виртуальное окружение активировано

## Быстрые ссылки

- [OpenRouter](https://openrouter.ai/) — регистрация и API ключ
- [Доступные модели](https://openrouter.ai/models) — список LLM моделей
- [Цены](https://openrouter.ai/models) — стоимость использования

---

**Готовы?** Запустите первый запрос и начните работу! 🚀

```bash
python -m src.main "Найди всё оборудование" --data-root ./data
```

