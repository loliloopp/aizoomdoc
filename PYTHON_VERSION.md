# Рекомендация по Python версии

## 🎯 Используйте Python 3.12 (Рекомендуется)

**Текущая конфигурация:** Python 3.13.5  
**Рекомендуемая конфигурация:** Python 3.12.x (LTS)

## 📊 Анализ совместимости

### Почему Python 3.12 лучше

1. **Стабильность** — выпущена 2 октября 2023, хорошо протестирована
2. **OpenCV** — лучшая поддержка на 3.12 (критично для обработки изображений)
3. **FastAPI/Pydantic** — полная совместимость
4. **Инструменты разработки** — mypy, black без проблем

### Риски Python 3.13.5

- ⚠️ OpenCV может работать нестабильно
- ⚠️ FastAPI/Pydantic не полностью протестированы на 3.13.5
- ⚠️ Возможные проблемы при масштабировании

## 🔧 Как переключиться

### Шаг 1: Установите Python 3.12

1. Скачайте с [python.org](https://www.python.org/downloads/release/python-3121/)
2. Установите (отметьте "Add Python 3.12 to PATH")
3. Убедитесь: `python --version` → должно быть 3.12.x

### Шаг 2: Пересоздайте окружение

```powershell
# Удалите старое окружение
rmdir /s venv

# Создайте новое (автоматически использует установленный Python)
python -m venv venv

# Активируйте
venv\Scripts\activate

# Переустановите зависимости
pip install -r requirements.txt
```

### Шаг 3: Проверьте версию

```powershell
python --version  # Должно быть 3.12.x
pip list          # Должны быть все пакеты
```

## 📝 Версионные ограничения в requirements.txt

Обновлённый `requirements.txt` теперь содержит верхние границы версий пакетов для гарантии совместимости:

```
Pillow>=10.0.0,<13.0.0
opencv-python>=4.8.0,<5.0.0
requests>=2.31.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
mypy>=1.7.0,<2.0.0
black>=23.12.0,<25.0.0
...
```

## ✅ После переключения

После установки Python 3.12 проект будет:
- ✅ Более стабильным
- ✅ Совместимым с OpenCV
- ✅ Лучше работать при обработке изображений
- ✅ Готовым для production использования

## ⚠️ Важно

Если вы используете на сервере или в CI/CD — убедитесь, что там установлен Python 3.12!

## 📚 Дополнительная информация

- [Python 3.12 EOL](https://peps.python.org/pep-0693/) — поддерживается до октября 2028
- [Python 3.13 status](https://peps.python.org/pep-0719/) — более новая, но менее стабильная

