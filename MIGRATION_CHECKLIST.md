# 📋 Чек-лист для миграции Python 3.13.5 → 3.12

## 🎯 Этапы

### Этап 1: Подготовка (5 минут)

- [ ] Прочитал `PYTHON_COMPATIBILITY_SUMMARY.md`
- [ ] Прочитал `PYTHON_VERSION_ANALYSIS.md`
- [ ] Понял, почему нужна миграция (OpenCV критичен для проекта)

### Этап 2: Установка Python 3.12 (10 минут)

**Вариант A: Автоматическая миграция (Рекомендуется)**

Windows:
```powershell
migrate_to_python312.bat
```

Linux/Mac:
```bash
chmod +x migrate_to_python312.sh
./migrate_to_python312.sh
```

**Вариант B: Вручную**

- [ ] Скачал Python 3.12 с https://python.org
- [ ] Установил Python 3.12 (отметил "Add to PATH")
- [ ] Удалил старую папку venv: `rmdir /s venv`
- [ ] Создал новую: `python -m venv venv`
- [ ] Активировал: `venv\Scripts\activate`
- [ ] Переустановил зависимости: `pip install -r requirements.txt`

### Этап 3: Проверка (5 минут)

- [ ] Проверил версию Python: `python --version` → должно быть 3.12.x
- [ ] Проверил OpenCV: `pip list | grep opencv` → должно быть есть
- [ ] Проверил FastAPI: `pip list | grep fastapi` → должно быть есть

### Этап 4: Тестирование (5 минут)

```powershell
# Проверьте, что всё работает
python -m src.main "test query" --data-root ./data
```

- [ ] Приложение запустилось без ошибок
- [ ] Нет предупреждений об OpenCV
- [ ] Логирование работает нормально

---

## 📊 Статус миграции

**Текущее состояние:** Python 3.13.5  
**Целевое состояние:** Python 3.12.x  
**Статус:** ⏳ В ожидании вашего действия

---

## 🔍 Важные файлы

| Файл | Описание |
|------|----------|
| `migrate_to_python312.bat` | Скрипт миграции для Windows |
| `migrate_to_python312.sh` | Скрипт миграции для Linux/Mac |
| `requirements.txt` | Обновлены версионные ограничения |
| `setup.py` | Требование Python >=3.12,<3.14 |
| `PYTHON_VERSION_ANALYSIS.md` | Детальный анализ совместимости |
| `PYTHON_COMPATIBILITY_SUMMARY.md` | Краткое резюме |

---

## ⚠️ КРИТИЧЕСКОЕ

**OpenCV 4.12.0.88 на Python 3.13.5 может:**
- ❌ Неправильно обрабатывать большие изображения (9932x7015)
- ❌ Потерять пиксели при создании viewport-кропов
- ❌ Вызвать случайные ошибки при рисовании подсветки

**Это потенциально нарушит ваше приложение!**

---

## ✅ Бонусы Python 3.12

- ✅ Стабильная обработка изображений
- ✅ Корректная работа Pydantic/FastAPI
- ✅ LTS поддержка до октября 2028
- ✅ Лучше подходит для production

---

## 🚀 Следующие шаги после миграции

1. **Проверить работоспособность приложения**
   ```powershell
   python -m src.main "Найди оборудование" --data-root ./data
   ```

2. **Если используете Docker/CI/CD**
   - Обновить базовый образ на Python 3.12
   - Обновить скрипты установки
   - Добавить проверку версии Python

3. **При развёртывании на сервер**
   - Убедиться, что на сервере установлен Python 3.12
   - Обновить документацию requirements

---

## 📞 Если возникли проблемы

1. **Проверьте версию Python:**
   ```powershell
   python --version
   ```

2. **Пересоздайте venv с нуля:**
   ```powershell
   rmdir /s venv
   python -m venv venv
   venv\Scripts\activate
   pip install -r requirements.txt
   ```

3. **Проверьте OpenCV:**
   ```powershell
   python -c "import cv2; print(cv2.__version__)"
   ```

4. **Посмотрите логи:**
   ```powershell
   type aizoomdoc.log
   ```

---

**Статус готовности:** ⏳ Ожидание действия пользователя

Запустите скрипт миграции и получите стабильное production-ready приложение! 🚀

