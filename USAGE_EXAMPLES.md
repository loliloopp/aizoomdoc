# Примеры использования AIZoomDoc

## Базовые примеры

### 1. Поиск всего вентиляционного оборудования

```bash
python -m src.main "Найди всё используемое вентиляционное оборудование на листах" --data-root ./data
```

**Что происходит:**
- Поиск по ключевым словам: "вентиляция", "вентилятор", "АОВ", "приточ", "вытяж" и т.д.
- Извлечение секций "Спецификация оборудования"
- Создание viewport-кропов для надписей на чертежах
- Отправка контекста в LLM

**Ожидаемый результат:**
```
РЕЗУЛЬТАТ АНАЛИЗА
================================================================================

РЕЗЮМЕ:
В проекте используется следующее вентиляционное оборудование:

1. Приточная установка П1
   - Источник: Лист 3, Спецификация оборудования
   - Характеристики: Расход 1000 м³/ч, Давление 250 Па

2. Вытяжной вентилятор АОВ8
   - Источник: Лист 5, План подвала, позиция на чертеже (Block ID: a4fb...)
   - Расположение: Подвал, помещение 1-05

...
```

### 2. Поиск конкретной позиции

```bash
python -m src.main "Найди на чертежах все упоминания АОВ8 и укажи их расположение" --data-root ./data
```

**Результат:**
- Точные координаты блоков с "АОВ8"
- Viewport-кропы с подсветкой
- Номера листов и помещений

### 3. Анализ характеристик оборудования

```bash
python -m src.main "Какие характеристики у вентиляторов по спецификации? Укажи расход, давление, мощность" --data-root ./data
```

**Результат:**
- Структурированная таблица с характеристиками
- Ссылки на листы спецификаций

## Сравнение стадий

### 4. Сравнение оборудования П vs Р

```bash
python -m src.main "Сравни стадию П и стадию Р по вентиляции. Найди отличия в оборудовании и трассах" \
  --stage-p ./data/stage_p \
  --stage-r ./data/stage_r
```

**Ожидаемый результат:**
```
РЕЗУЛЬТАТ АНАЛИЗА
================================================================================

СРАВНЕНИЕ СТАДИЙ П И Р

=== СТАДИЯ П ===

Оборудование:
1. Приточная установка П1 - 1000 м³/ч
2. Вытяжной вентилятор АОВ8 - 500 м³/ч
...

=== СТАДИЯ Р ===

Оборудование:
1. Приточная установка П1 - 1200 м³/ч (ИЗМЕНЕНО)
2. Вытяжной вентилятор АОВ8 - 500 м³/ч
3. Дополнительный вентилятор В1 - 300 м³/ч (ДОБАВЛЕНО)
...

=== ОТЛИЧИЯ ===

1. ИЗМЕНЕНИЕ ХАРАКТЕРИСТИК:
   - П1: Расход увеличен с 1000 до 1200 м³/ч
   - Источник: Стадия П - Лист 3, Стадия Р - Лист 4

2. ДОБАВЛЕНО ОБОРУДОВАНИЕ:
   - В1: Новый вытяжной вентилятор 300 м³/ч
   - Расположение: Этаж 2, помещение 2-12
   - Источник: Стадия Р - Лист 7

3. ИЗМЕНЕНИЯ В ТРАССИРОВКЕ:
   - Трасса воздуховода к П1 изменена (видно на Листе 5 стадии Р)
...
```

### 5. Сравнение конкретных параметров

```bash
python -m src.main "Сравни суммарную мощность вентиляционного оборудования в стадии П и Р" \
  --stage-p ./data/stage_p \
  --stage-r ./data/stage_r
```

## Продвинутые примеры

### 6. Выбор конкретной модели

```bash
# Использование Claude для более детального анализа
python -m src.main "Проанализируй вентиляционную систему и дай рекомендации по оптимизации" \
  --data-root ./data \
  --model "anthropic/claude-3.5-sonnet"
```

### 7. Сохранение результата в файл

```bash
python -m src.main "Полный анализ вентиляции" \
  --data-root ./data \
  --output ./reports/ventilation_analysis.txt
```

### 8. Детальный анализ с логами

```bash
# Установите LOG_LEVEL=DEBUG в .env
python -m src.main "Найди все вентиляторы" --data-root ./data
```

В логах будет видно:
- Какие ключевые слова использованы
- Сколько блоков найдено
- Координаты viewport-кропов
- Параметры запроса к LLM

## Типовые запросы по категориям

### Поиск оборудования

```bash
# Вентиляция
python -m src.main "Найди всё вентиляционное оборудование" --data-root ./data

# Отопление (если есть соответствующие ключевые слова)
python -m src.main "Найди всё отопительное оборудование и радиаторы" --data-root ./data

# Конкретная марка
python -m src.main "Найди все вентиляторы марки VTS" --data-root ./data
```

### Анализ характеристик

```bash
# Расход воздуха
python -m src.main "Какой общий расход воздуха по приточным установкам?" --data-root ./data

# Мощность
python -m src.main "Какая суммарная мощность вентиляционного оборудования?" --data-root ./data

# Параметры трубопроводов
python -m src.main "Найди все воздуховоды и укажи их диаметры" --data-root ./data
```

### Локализация на чертежах

```bash
# Поиск по помещению
python -m src.main "Какое вентиляционное оборудование установлено в подвале?" --data-root ./data

# Поиск по листу
python -m src.main "Что показано на листе 5 по вентиляции?" --data-root ./data

# Поиск по оси
python -m src.main "Найди оборудование между осями А-Г и 1-5" --data-root ./data
```

### Сравнение стадий

```bash
# Общее сравнение
python -m src.main "Какие изменения в вентиляции между стадией П и Р?" \
  --stage-p ./data/stage_p --stage-r ./data/stage_r

# Сравнение количества
python -m src.main "Изменилось ли количество вентиляторов от П к Р?" \
  --stage-p ./data/stage_p --stage-r ./data/stage_r

# Сравнение расположения
python -m src.main "Изменилось ли расположение вентиляционного оборудования?" \
  --stage-p ./data/stage_p --stage-r ./data/stage_r
```

## Интерпретация результатов

### Структура ответа LLM

LLM обучен отвечать в следующем формате:

1. **РЕЗЮМЕ** - краткая сводка
2. **ДЕТАЛЬНАЯ ИНФОРМАЦИЯ** - пункты с подробностями
3. **ИСТОЧНИКИ** - ссылки на листы и block_id
4. **ОГРАНИЧЕНИЯ** - что не удалось найти (если применимо)

### Пример ответа с недостаточными данными

```
РЕЗУЛЬТАТ АНАЛИЗА
================================================================================

РЕЗЮМЕ:
Найдена частичная информация о вентиляционном оборудовании.

НАЙДЕННАЯ ИНФОРМАЦИЯ:
1. Приточная установка П1 (Лист 3)
   - Расход: 1000 м³/ч
   - Мощность: информация отсутствует

ОГРАНИЧЕНИЯ:
- В предоставленных документах не найдена информация о давлении вентиляторов
- Отсутствуют данные о производителе оборудования
- Трассировка воздуховодов на чертежах не полностью читаема из-за качества OCR

РЕКОМЕНДАЦИИ:
Для полного анализа необходимы дополнительные листы спецификаций.
```

## Советы по формулированию запросов

### ✅ Хорошие запросы

- "Найди всё вентиляционное оборудование на листах"
- "Сравни характеристики вентиляторов в стадии П и Р"
- "Какой расход воздуха у установки П1?"
- "Где на чертежах расположен вентилятор АОВ8?"

### ❌ Плохие запросы

- "Что тут есть?" (слишком общо)
- "Нарисуй мне схему" (LLM не может рисовать)
- "Почему так спроектировано?" (требует домыслов)
- "Исправь ошибки" (требует редактирования файлов)

### 💡 Улучшение запросов

Плохо: "Расскажи про вентиляцию"
Хорошо: "Перечисли всё вентиляционное оборудование с характеристиками и расположением"

Плохо: "Что изменилось?"
Хорошо: "Сравни стадию П и Р: какие добавлены/удалены вентиляторы и как изменились их характеристики?"

## Устранение проблем

### Проблема: "Результат поиска пуст"

**Причина:** Ключевые слова не найдены в документах

**Решение:**
1. Проверьте, что `result.md` содержит текст
2. Попробуйте более общие ключевые слова
3. Проверьте язык (русский/английский)

### Проблема: "Изображение страницы не найдено"

**Причина:** Отсутствует файл `page_XXX_full.jpg`

**Решение:**
1. Проверьте наличие изображений в `data_root`
2. Убедитесь в правильном формате имён: `page_001_full.jpg`

### Проблема: "LLM возвращает неточный ответ"

**Причина:** Контекст слишком большой или недостаточный

**Решение:**
1. Уточните запрос (более конкретные ключевые слова)
2. Попробуйте другую модель (Claude vs Gemini)
3. Проверьте качество OCR в `result.md`

### Проблема: "Таймаут при запросе к LLM"

**Причина:** Слишком много изображений или медленная сеть

**Решение:**
1. Уменьшите `VIEWPORT_SIZE` в `.env`
2. Используйте более быструю модель
3. Уточните запрос для меньшего количества результатов

